<div class="card mt-5 mx-3">
    <div class="card-header d-flex justify-content-between align-items-center">
        <div><h3 class="m-0">{{blogs.blog_title}}</h3></div>
        <div><h5 class="m-0">Posted by {{blogs.user.username}} on {{format_date blogs.date_created}} </h5></div>
    </div>
    <div class="card-body">
        <h4>{{blogs.blog_description}}</h4>
    </div>
</div>
